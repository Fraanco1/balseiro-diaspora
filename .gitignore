__pycache__/
*.pyc
.DS_Store

# Cached API responses — regenerated by scripts/run_all.py.
# Keep them if you want reproducible builds without re-fetching; ignore by default.
data/raw/

# site/data/alumni.json is committed (GitHub Pages serves it).
# The pretty-printed copy is a local debugging aid only.
site/data/alumni.pretty.json
